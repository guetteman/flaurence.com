name: flaurence
php: '8.3'
secured: true
aliases:
  - flaurence
services:
    postgresql:
        version: '16'
        port: '${DB_PORT}'
    redis:
        version: 7.0.0
        port: '${REDIS_PORT}'
integrations:
    forge: {  }
